[project]
name = "ytplayer"
version = "0.1.0"
description = "A free, open-source video and audio player with a clean, modern UI — built with Python and PyQt6."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "clipboard>=0.0.4",
    "pyinstaller>=6.19.0",
    "pyqt6>=6.10.2",
]
